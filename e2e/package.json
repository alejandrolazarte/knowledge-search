{
  "name": "knowledge-search-e2e",
  "private": true,
  "packageManager": "pnpm@11.0.0",
  "scripts": {
    "test":             "playwright test",
    "install:browsers": "playwright install --with-deps chromium",
    "report":           "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
